<!-- 项目变更记录：按版本记录功能、修复和迁移进度。 -->

# 更新日志

## 2026-08-26

### PySide6 界面迁移

- 新增基于 PySide6 的主界面 `gui_pyside6.py`，作为 Tkinter 版 `gui.py` 的新版实现。
- 将主窗口、任务编辑器、预设管理、日志输出和执行控制迁移到 Qt 控件体系，保留原有任务数据和预设兼容能力。
- 使用 `QThread`、Qt 信号和槽承载脚本执行线程，实现日志、当前步骤、执行结果和完成状态的线程安全回传。
- 统一支持开始执行、从当前步骤执行、停止、暂停、继续和单步执行，并在运行期间同步更新控件状态。

### 任务列表与编辑器

- 任务列表由 Tkinter `Listbox` 升级为 `QTreeWidget`，支持分组层级、组状态回填、多选、勾选启用、回车切换和拖放排序。
- 新增 Qt 委托绘制的任务启用状态与拖动手柄，任务排序操作不再依赖整行拖动。
- 将通用步骤设置和类型专用设置整合到可滚动的 Qt 编辑面板，支持普通、高级、持续点击、延迟、按键、键盘移动、拖曳、条件、选择、循环和事件步骤。
- 支持在主界面直接编辑步骤名称、启用状态、模板、阈值、超时、等待方式、点击坐标、识别区域、偏移和类型专用字段。
- 组设置迁移到 Qt 表单，支持组名称、颜色、启用状态和组内步骤同步更新。

### Qt 蓝图画布

- 使用 `QGraphicsScene` 和 `QGraphicsView` 重写蓝图编辑器，节点、组、连线和转折点改为独立图形项。
- 蓝图节点采用 Qt 绘制，支持运行状态着色、连接点状态高亮、节点折叠、组容器和组头交互。
- 连线改用 `QGraphicsPathItem` 绘制，支持执行流箭头、分支颜色、折线路径、多转折点、转折点拖动和连线选择高亮。
- 支持鼠标中键平移、Ctrl+滚轮缩放、框选、多选、节点和组拖动，以及 Ctrl+Z、Ctrl+Y、Ctrl+C、Ctrl+V 和 Delete 快捷操作。
- 折叠节点或分组后，蓝图会自动隐藏内部元素并将外部连接收束到可见节点或组头。
- 蓝图右侧编辑器可直接修改当前节点或组，减少在主窗口和蓝图窗口之间切换的操作。

### 屏幕采集与资源管理

- 新增基于 Qt 顶层透明窗口的屏幕采集覆盖层，支持点击记录、识别区域框选和模板图片框选。
- 屏幕采集支持 Esc 取消、实时选区预览、窗口模式坐标换算，并将截取图片保存到 `icons` 后自动重新加载模板。
- 预设导入导出在 PySide6 界面中提供完整 ZIP 操作，包含任务、分组、蓝图布局、蓝图连接和引用图片的恢复。

### 兼容与迁移说明

- PySide6 界面继续复用 `tasks.py`、`nodes.py`、`main.py` 和 `config.py` 的任务、节点图、执行器及配置接口。
- 保留旧版 Tkinter 界面 `gui.py`，两个界面共享任务和预设存储格式，可按启动脚本选择使用的界面。
- 新版界面仍沿用旧任务字段和旧 JSON 数据结构，打开历史任务时自动兼容节点 ID、分组、布局及蓝图数据。

## 2026-08-24

### 蓝图分组

- 将主界面的组逻辑同步到蓝图编辑器。
- 蓝图支持组容器、组收起/展开和组整体拖动。
- 展开组使用透明背景，仅组头显示组颜色；收起组显示组名和步骤数量。
- 收起组后隐藏组内连线，连接外部步骤的连线保留并连接到组头。
- 组内步骤支持单独拖动，不再受组容器影响。
- 蓝图组右键支持新增组、编辑组设置和删除组。
- 选中蓝图组时，右侧显示组名称、颜色和展开状态设置。
- 蓝图步骤支持加入任意组，也支持移出当前组并放到根组。
- 组颜色改为预设颜色表选择。
- 蓝图组布局和状态随当前预设保存。

### 步骤与识别

- 图像识别步骤增加匹配阈值设置，默认继承 `config.py` 中的 `THRESHOLD`。
- 普通、高级、条件、事件和持续点击等图像识别步骤支持单独覆盖匹配阈值。
- 组内新增步骤自动加入对应组，组外新增步骤保持无组。
- 空预设新增步骤时不再自动创建默认组。

### 执行控制

- 修复暂停后单步执行无效的问题。
- 暂停状态下每次点击“单步”只执行一个完整步骤，执行结束后自动恢复暂停。
- 暂停状态会响应识别轮询、等待和延时。

### 术语统一

- 统一将实际执行单位称为“步骤”。
- 统一将蓝图步骤两侧用于建立连线的小圆点称为“连接点”。

## 2026-08-23

### 蓝图编辑器

- 新增独立蓝图窗口，左侧为流程画布，右侧为步骤设置。
- 步骤支持拖动、框选、多选和整体移动。
- 支持 Ctrl+点击追加或取消选择。
- 支持鼠标中键平移画布。
- 支持 Ctrl+鼠标滚轮缩放。
- 步骤支持折叠/展开、重命名、自定义颜色和步骤注释。
- 支持自动排列、选中步骤对齐和网格吸附。
- 支持 Ctrl+Z 撤销、Ctrl+Y 重做、Ctrl+C 复制和 Ctrl+V 粘贴。
- 复制粘贴多个步骤时保留内部连接。
- 双击步骤可快速折叠或展开。
- 已连接连接点保持高亮原色并带白色描边，未连接连接点保留原色但降低亮度。
- 修复蓝图快捷键失效问题，快捷键由蓝图窗口统一接收。

### 连接与分支

- 支持普通顺序连接。
- 支持识别成功、未识别和超时分支。
- 支持条件、选择、循环和事件步骤的分支出口。
- 支持右键删除连接。
- 支持在线上直接添加多个转折点。
- 转折点支持拖动调整线形和右键删除。
- 转折点仅在线路被选中时显示。
- 折叠步骤后，普通线、迂回线和超时线会合并到步骤中心。
- 蓝图连接可以应用到主界面，并重建主流程顺序。
- 应用前检查非法目标、自连接和循环连接。
- 折叠步骤后，普通线、迂回线和超时线会统一收束到步骤中心。
- 连线转折点改为直接在画布上添加，不再需要输入坐标。
- 同一条连线支持多个转折点，转折点可拖动调整，右键可删除。
- 转折点仅在选中对应连线时显示。

### 步骤类型

- 新增条件步骤，支持模板识别条件和 AND、OR、NOT 运算。
- 新增 Switch/选择步骤，支持静态值分支和默认出口。
- 新增循环步骤，支持固定次数、循环体和退出目标。
- 新增延迟步骤，延迟期间支持停止脚本。
- 新增事件步骤，支持等待模板出现和超时跳转。
- 普通步骤支持识别成功、未识别和等待超时独立出口。
- 新增独立事件节点设置和条件节点多模板运算配置。

### 执行与调试

- 执行器在存在蓝图连接时从入口节点开始，沿蓝图路径执行。
- 没有蓝图连接的旧任务继续按列表顺序执行。
- 增加暂停、继续、单步和从当前步骤执行。
- 运行时高亮当前步骤和实际经过的主线连线。
- 步骤显示运行中、成功和失败状态。
- 新增蓝图完整性、循环和不可达节点检查。
- 增加执行回调，便于后续扩展调试和运行监控。

### 数据与架构

- 新增 `Node`、`TaskNode` 和 `NodeGraph` 统一节点适配层。
- 节点图支持统一的 `execute`、`validate`、`inputs` 和 `outputs` 接口。
- 节点与连接保存到 `saved_blueprint_graphs.json`。
- 蓝图布局和缩放保存到 `saved_blueprint_layouts.json`。
- 任务加载时自动补齐旧配置缺少的节点 ID、类型和启用状态。
- 保留旧版任务 JSON 兼容导入。
- 修复蓝图存储函数错误缩进导致的 `ImportError` 启动问题。
- 原生节点和连接模型保存到独立的 `saved_blueprint_graphs.json`。
- 任务字典加载时自动迁移为带稳定 ID 的节点数据。

### 文档与验证

- 更新 `README.md`，补充蓝图、节点、连接、调试和数据文件说明。
- 更新 `待办.txt`，同步基础连接、核心执行、条件系统、编辑体验和架构优化进度。
- 通过 Python 编译检查和编辑器代码诊断。
- 现有任务执行回归测试通过；图像模板匹配测试偶发存在 1 像素坐标波动。

### 预设导入导出

- 新增“导出预设”功能，将当前预设导出为 ZIP。
- 导出内容包括全部步骤、步骤设置、分组、蓝图连接、蓝图布局和绑定 PNG 图片。
- 新增“导入预设”功能，可自定义导入后的预设名称或覆盖同名预设。
- 导入时自动将 ZIP 中的 PNG 图片恢复到本地 `icons` 文件夹。
- 导入过程只解压 PNG 图片，不释放其它文件到工程目录。
